---
- name: Configure drone server (Docker + Nginx + Let's Encrypt)
  hosts: all
  become: true
  gather_facts: true

  roles:
    - role: common
      tags: [common]

    - role: docker
      tags: [docker]

    - role: nginx
      tags: [nginx]

    # - role: certbot
    #   tags: [certbot]